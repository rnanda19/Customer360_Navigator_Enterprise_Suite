# src/models/
Model wrappers for the classical 6-model benchmark set (BP3/BP5/BP7) and the NLP classifiers/embedding
wrappers (BP1/BP2/BP6), plus the shared CV harness (StratifiedKFold, seed=42, identical folds across models).
