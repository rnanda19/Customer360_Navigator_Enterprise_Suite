# src/utils/
performance_setup.py (WARP: configure_performance() called first, before any heavy import - see
LESSONS_LEARNED_APPLIED.md #5), path resolution helpers, psutil core-affinity pinning, the hardware benchmark
harness shared by notebooks/00_hardware_benchmark/.
