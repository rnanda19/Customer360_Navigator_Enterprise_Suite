# src/reporting/
Shared report_builder module: batched python-docx writer, reused matplotlib figures, vectorized openpyxl
writer with a dedicated Assumptions sheet, and a self-contained Chart.js HTML dashboard builder
(bundle Chart.js locally - do not rely on a CDN, per the Home Credit Notebook 01 real-run lesson).
