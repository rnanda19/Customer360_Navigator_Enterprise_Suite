# BP7 - Customer Navigator Decision Engine - reports

Gate 6 governance set: `MODEL_CARD.md`, `CHANGELOG.md`, FastAPI self-test report (`/decide/self-test`, no live external call - this BP never makes a network call). `executive_rollup/` holds the real dashboard/report/workbook/deck.

**Production status: RECOMMENDED FOR PRODUCTION** (pytest 410/410; ECOA/Reg B check run and passed, `adverse_impact_ratio=0.908127`).
